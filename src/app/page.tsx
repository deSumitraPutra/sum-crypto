'use client'

import { Header } from "@/components/Header";
import { Table } from "@/components/Table";

const Home = () => {
  return (
    <main>
      <Header />
      <Table />
    </main>
  )
};

export default Home;
